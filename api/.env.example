APP_DEBUG=false

DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=restaurante
DB_USERNAME=root
DB_PASSWORD=

# Gere com: php -r "echo bin2hex(random_bytes(32));"
JWT_SECRET=
JWT_TTL=28800

CORS_ORIGIN=*
# Só precisa se a detecção automática do prefixo da URL falhar (ex.: /restaurante/api)
# BASE_PATH=/restaurante/api
